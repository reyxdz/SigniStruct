# Document Signer - Architecture & Flow Diagrams

## 1. SYSTEM ARCHITECTURE DIAGRAM

```
┌─────────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React)                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │
│  │  Dashboard   │  │  Document    │  │   Signature  │             │
│  │   Page       │──│ Management   │──│  Creation    │             │
│  │              │  │   Page       │  │    Page      │             │
│  └──────────────┘  └──────────────┘  └──────────────┘             │
│         │                  │                  │                    │
│         └──────────────────┼──────────────────┘                    │
│                            │                                       │
│                  ┌─────────▼─────────┐                             │
│                  │ Document Signer   │                             │
│                  │ Page              │                             │
│                  │ - Viewer          │                             │
│                  │ - Placement Tool  │                             │
│                  │ - Signature Pad   │                             │
│                  └─────────┬─────────┘                             │
│                            │                                       │
│                  ┌─────────▼─────────┐                             │
│                  │ API Service       │                             │
│                  │ (axios)           │                             │
│                  └─────────┬─────────┘                             │
└────────────────────────────┼─────────────────────────────────────┘
                             │
                             │ HTTPS/REST
                             │
┌────────────────────────────▼─────────────────────────────────────┐
│                     BACKEND (Express.js)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐   │
│  │ Auth Routes    │  │ Certificate    │  │  Document     │   │
│  │ (signin/up)    │  │ Routes         │  │  Routes       │   │
│  └────────────────┘  └────────────────┘  └────────────────┘   │
│         │                    │                    │            │
│         └────────────────────┼────────────────────┘            │
│                              │                                 │
│         ┌────────────────────▼────────────────────┐             │
│         │    PKI Service                          │             │
│         │  ┌──────────────────────────────────┐  │             │
│         │  │ - Generate Key Pairs (RSA-2048)  │  │             │
│         │  │ - Create Certificates            │  │             │
│         │  │ - Sign Documents                 │  │             │
│         │  │ - Verify Signatures              │  │             │
│         │  └──────────────────────────────────┘  │             │
│         └────────────────────┬────────────────────┘             │
│                              │                                 │
│         ┌────────────────────▼────────────────────┐             │
│         │    Crypto Services                      │             │
│         │  ┌──────────────────────────────────┐  │             │
│         │  │ - AES-256 Encryption             │  │             │
│         │  │ - SHA-256 Hashing                │  │             │
│         │  │ - RSA Signing/Verification       │  │             │
│         │  │ - Key Derivation                 │  │             │
│         │  └──────────────────────────────────┘  │             │
│         └────────────────────┬────────────────────┘             │
│                              │                                 │
│         ┌────────────────────▼────────────────────┐             │
│         │    File Handling Services               │             │
│         │  ┌──────────────────────────────────┐  │             │
│         │  │ - Upload Document                │  │             │
│         │  │ - Generate Hash                  │  │             │
│         │  │ - Store/Retrieve Files           │  │             │
│         │  └──────────────────────────────────┘  │             │
│         └────────────────────┬────────────────────┘             │
└────────────────────────────┬─────────────────────────────────┘
                             │
                             │ Mongoose/MongoDB Driver
                             │
┌────────────────────────────▼─────────────────────────────────────┐
│                      MONGODB (Database)                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │     users    │  │   documents  │  │ user_        │          │
│  │              │  │              │  │ signatures   │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   users_     │  │   document_  │  │   signature_ │          │
│  │ certificates │  │  signatures  │  │  audit_log   │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. PKI CERTIFICATE LIFECYCLE

```
┌─────────────────────────────────────────────────────────────────┐
│                    CERTIFICATE LIFECYCLE                        │
└─────────────────────────────────────────────────────────────────┘

USER REGISTRATION
       │
       ▼
┌──────────────────────┐
│ Generate Key Pair    │
│ - RSA 2048-bit       │
│ - Public Key         │
│ - Private Key        │
└──────────────────────┘
       │
       ▼
┌──────────────────────┐
│ Create Self-Signed   │
│ Certificate          │
│ - Subject: User info │
│ - Serial number      │
│ - Validity: 1-5 yrs  │
│ - Algorithm: SHA-256 │
└──────────────────────┘
       │
       ▼
┌──────────────────────┐
│ Encrypt Private Key  │
│ - Algorithm: AES-256 │
│ - Key: Master Key    │
└──────────────────────┘
       │
       ▼
┌──────────────────────┐
│ Store in Database    │
│ - Public cert (PEM)  │
│ - Private key (enc)  │
│ - Metadata           │
└──────────────────────┘
       │
       ▼
    ACTIVE
    (User can sign)
       │
       ├─────────────────────────┐
       │                         │
     (1 year)            (Manual revocation)
       │                         │
       ▼                         ▼
┌──────────────┐        ┌──────────────┐
│  EXPIRING    │        │   REVOKED    │
│              │        │              │
│ Notify user  │        │ No longer    │
│  Renew?      │        │ valid        │
└──────────────┘        └──────────────┘
       │
       ├── Yes ──→ Generate new pair
       │
       └── No ──→ EXPIRED (archive)
```

---

## 3. DOCUMENT SIGNING FLOW (WITH PKI)

```
┌──────────────────────────────────────────────────────────────────┐
│              END-TO-END DOCUMENT SIGNING FLOW                    │
└──────────────────────────────────────────────────────────────────┘

1. PREPARATION
   ┌─────────────────┐
   │ Upload Document │
   └────────┬────────┘
            │
            ▼
   ┌─────────────────────────┐
   │ Generate File Hash      │
   │ Algorithm: SHA-256      │
   │ Result: document_hash   │
   └────────┬────────────────┘
            │
            ▼
   ┌─────────────────────────┐
   │ Document READY TO SIGN  │
   │ Status: pending_signature
   └────────┬────────────────┘
            │

2. SIGNATURE CREATION
            │
            ▼
   ┌─────────────────────────────────┐
   │ User Views Document             │
   │ - Select signature placement    │
   │ - Position: (x, y, page)        │
   │ - Size: width, height           │
   └────────┬────────────────────────┘
            │
            ▼
   ┌─────────────────────────────────┐
   │ Create Signature Image          │
   │ - Pad: User draws signature     │
   │ OR                              │
   │ - Upload: Pre-drawn signature   │
   │ Result: signature_image (b64)   │
   └────────┬────────────────────────┘
            │

3. CRYPTOGRAPHIC SIGNING
            │
            ▼
   ┌─────────────────────────────────┐
   │ Retrieve User's Keys            │
   │ - Get certificate from DB       │
   │ - Decrypt private key           │
   └────────┬────────────────────────┘
            │
            ▼
   ┌─────────────────────────────────┐
   │ Sign Document Hash              │
   │ Algorithm: RSA-PSS              │
   │ - Input: document_hash          │
   │ - Key: private_key              │
   │ - Output: signature_hash (b64)  │
   └────────┬────────────────────────┘
            │

4. SIGNATURE ENVELOPE CREATION
            │
            ▼
   ┌─────────────────────────────────────┐
   │ Create Signature Envelope           │
   │ {                                   │
   │   "document_id": "...",             │
   │   "signer_id": "...",               │
   │   "signature_image": "b64...",      │
   │   "signature_hash": "b64...",       │
   │   "certificate_id": "...",          │
   │   "certificate_fingerprint": "...", │
   │   "position": {page, x, y, w, h},   │
   │   "timestamp": "2026-02-23T...",    │
   │   "ip_address": "...",              │
   │   "user_agent": "..."               │
   │ }                                   │
   └────────┬────────────────────────────┘
            │

5. STORAGE & FINALIZATION
            │
            ▼
   ┌─────────────────────────────────┐
   │ Store to Database               │
   │ - Save signature envelope       │
   │ - Update document status        │
   │ - Create audit log entry        │
   └────────┬────────────────────────┘
            │
            ▼
   ┌─────────────────────────────────┐
   │ DOCUMENT SIGNED ✓               │
   │ Status: signed                  │
   │ Can now be downloaded/shared    │
   └─────────────────────────────────┘
```

---

## 4. SIGNATURE VERIFICATION FLOW

```
┌──────────────────────────────────────────────────────────────────┐
│           SIGNATURE VERIFICATION AND VALIDATION FLOW             │
└──────────────────────────────────────────────────────────────────┘

INPUT: Signed Document + Signature Envelope
       │
       ▼
┌──────────────────────────────────────────┐
│ Extract Signature from Document          │
│ - Get signature_hash                     │
│ - Get certificate_id                     │
│ - Get signature_image                    │
│ - Get timestamp                          │
└──────┬───────────────────────────────────┘
       │
       ├────────────────────────────────────────┐
       │                                        │
       ▼                                        ▼
┌──────────────────────┐          ┌──────────────────────┐
│ STEP 1: HASH CHECK   │          │ STEP 2: CERT CHECK   │
├──────────────────────┤          ├──────────────────────┤
│ Regenerate File Hash │          │ Get Certificate      │
│ - Read doc file      │          │ - From database      │
│ - SHA-256 hash       │          │ Extract public key   │
│ (should match orig)  │          │                      │
└──────┬───────────────┘          └──────┬───────────────┘
       │                                  │
       ▼                                  ▼
┌──────────────────────┐          ┌──────────────────────┐
│ Verify Hash Match    │          │ Check Validity       │
│ regenerated_hash ==  │          │ - Not expired?       │
│ stored_hash?         │          │ - Not revoked?       │
│ ✓ / ✗                │          │ ✓ / ✗                │
└──────┬───────────────┘          └──────┬───────────────┘
       │                                  │
       ▼                                  ▼
┌──────────────────────────────────────────┐
│       STEP 3: SIGNATURE VERIFICATION     │
├──────────────────────────────────────────┤
│                                          │
│ RSA Verify:                              │
│   public_key (from certificate)          │
│   ✓ signature_hash (from envelope)       │
│   ✓ regenerated_hash (from file)         │
│                                          │
│ Verification Algorithm:                  │
│   decrypt(signature_hash, pub_key)       │
│   == regenerated_hash ?                  │
│   ✓ / ✗                                  │
│                                          │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│       STEP 4: TIMESTAMP VALIDATION       │
├──────────────────────────────────────────┤
│                                          │
│ - Check timestamp is reasonable          │
│ - Not in future?                         │
│ - Not too old? (if req'd)                │
│ - NTP verification (if enabled)          │
│ ✓ / ✗                                    │
│                                          │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│        FINAL VERDICT                     │
├──────────────────────────────────────────┤
│                                          │
│ ✓ ALL CHECKS PASSED?                    │
│   - Hash match: ✓                        │
│   - Certificate valid: ✓                 │
│   - Signature verified: ✓                │
│   - Timestamp valid: ✓                   │
│                                          │
│ → RESULT: DOCUMENT AUTHENTIC & VALID     │
│                                          │
│ ✗ ANY CHECK FAILED?                     │
│   → RESULT: DOCUMENT TAMPERED/INVALID    │
│                                          │
└──────────────────────────────────────────┘

OUTPUT:
  {
    "is_valid": true/false,
    "signature_valid": true/false,
    "certificate_valid": true/false,
    "hash_match": true/false,
    "verified_at": "timestamp",
    "details": { ... }
  }
```

---

## 5. MULTI-SIGNER WORKFLOW

```
┌────────────────────────────────────────────────────────────────────┐
│            MULTI-SIGNER DOCUMENT WORKFLOW                         │
└────────────────────────────────────────────────────────────────────┘

DOCUMENT CREATED by Owner
       │
       ▼
┌──────────────────────────┐
│ Add Signers              │
│ [  ] User A              │
│ [  ] User B              │
│ [  ] User C              │
│ Signing Order: Sequential │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│ Send Signing Requests                    │
│ - User A: "Please sign document XYZ"     │
│ - Email notification sent                │
│ - Signing link provided                  │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│ USER A: SIGNS DOCUMENT (1/3)             │
│ - Opens signing link                     │
│ - Places signature                       │
│ - Clicks "SIGN"                          │
│ → Signature stored                       │
│ → Next signer notified                   │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│ USER B: SIGNS DOCUMENT (2/3)             │
│ - Opens signing link                     │
│ - Can see User A's signature             │
│ - Places their signature                 │
│ - Clicks "SIGN"                          │
│ → Signature stored                       │
│ → Next signer notified                   │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│ USER C: SIGNS DOCUMENT (3/3)             │
│ - Opens signing link                     │
│ - Can see A & B's signatures             │
│ - Places their signature                 │
│ - Clicks "SIGN"                          │
│ → Final signature stored                 │
│ → Document completion triggered          │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│ DOCUMENT FULLY SIGNED ✓                  │
│ All signatories verified                 │
│ Proof of signing order recorded          │
│                                          │
│ Owner receives:                          │
│ - Fully signed document                  │
│ - Signature receipts (3)                 │
│ - Audit trail                            │
│ - Download/Archive options               │
└──────────────────────────────────────────┘
```

---

## 6. SECURITY: KEY MANAGEMENT

```
┌─────────────────────────────────────────────────────┐
│      PRIVATE KEY ENCRYPTION & STORAGE              │
└─────────────────────────────────────────────────────┘

DURING CERTIFICATE GENERATION:
┌────────────────────┐
│ Generate Key Pair  │
│ RSA 2048-bit       │
└────────┬───────────┘
         │
         ▼
┌─────────────────────────────────┐
│ Private Key (plaintext)         │
│ -----BEGIN RSA PRIVATE KEY----- │
│ MIIEvQIBADANBgkqhkiG9w0BAQEF...│
│ -----END RSA PRIVATE KEY-------  │
└────────┬────────────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ ENCRYPT WITH AES-256-CBC            │
│                                      │
│ Key Derivation:                      │
│   - Master Encryption Key (from env) │
│   - + User ID (PBKDF2)               │
│   - = Unique cipher key              │
│                                      │
│ Encryption:                          │
│   encrypted = AES.encrypt(           │
│     plaintext_key,                   │
│     cipher_key,                      │
│     iv                               │
│   )                                  │
│                                      │
│ Result: encrypted_private_key        │
│ (Base64 string)                      │
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ STORE IN DATABASE                    │
│                                      │
│ users_certificates {                │
│   _id: ObjectId,                     │
│   user_id: ObjectId,                 │
│   public_key: "-----BEGIN PUBLIC...", │
│   private_key_encrypted: "U2FsdG...", │
│   certificate_pem: "-----BEGIN...",   │
│   status: "active"                   │
│ }                                    │
│                                      │
└────────┬─────────────────────────────┘
         │

WHEN USER NEEDS TO SIGN:
         │
         ▼
┌────────────────────────────────┐
│ User initiates signing         │
│ Server authenticates user      │
│ (JWT verified)                 │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ Retrieve encrypted_private_key │
│ from database                  │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────────────┐
│ DECRYPT (temporary, in-memory)         │
│                                        │
│ plaintext_key = AES.decrypt(           │
│   encrypted_private_key,               │
│   cipher_key,                          │
│   iv                                   │
│ )                                      │
│                                        │
│ (Key only exists in memory for signing)│
└────────┬───────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────┐
│ USE PRIVATE KEY TO SIGN DOCUMENT       │
│                                        │
│ signature = RSA.sign(                  │
│   document_hash,                       │
│   plaintext_key                        │
│ )                                      │
│                                        │
└────────┬───────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────┐
│ CLEAR FROM MEMORY                      │
│ - plaintext_key = null                 │
│ - Delete all references                │
│ - Force garbage collection             │
│                                        │
│ (Key never exposed on disk)            │
│                                        │
└────────────────────────────────────────┘
```

---

## 7. DATABASE RELATIONSHIPS

```
┌──────────────────────────────────────────────────────────────────────┐
│              DOCUMENT SIGNER DATABASE SCHEMA                         │
└──────────────────────────────────────────────────────────────────────┘


                            ┌─────────────┐
                            │    users    │
                            ├─────────────┤
                            │ _id (PK)    │
                            │ email       │
                            │ firstName   │
                            │ lastName    │
                            │ phone       │
                            │ password    │
                            │ ...         │
                            └────┬────────┘
                                 │
                 ┌───────────────┼───────────────┐
                 │               │               │
                 │ (1 to ∞)      │ (1 to ∞)      │ (1 to ∞)
                 ▼               ▼               ▼
        ┌──────────────────┐ ┌──────────────┐ ┌─────────────┐
        │users_certificates│ │user_signatures│ │  documents  │
        ├──────────────────┤ ├──────────────┤ ├─────────────┤
        │_id (PK)          │ │_id (PK)      │ │_id (PK)     │
        │user_id (FK)◄─────┼─┤user_id (FK)◄─┼─┤owner_id (FK)│
        │public_key        │ │signature_img │ │title        │
        │private_key_enc   │ │type          │ │file_url     │
        │certificate_pem   │ │is_default    │ │status       │
        │status            │ │created_at    │ │created_at   │
        │not_after         │ └──────────────┘ └─────┬───────┘
        │fingerprint       │                        │
        │...               │                   (1 to ∞)
        └──────┬───────────┘                        │
               │                                    ▼
               │ (1 to ∞)                   ┌──────────────────┐
               │                        ┌───┤document_signatures│
               │                        │   ├──────────────────┤
               │                        │   │_id (PK)          │
               │                        │   │document_id (FK)◄─┤
               ▼                        │   │signer_id (FK)    │
        ┌──────────────────┐           │   │signature_image   │
        │document_signatures│───────┐  │   │signature_hash    │
        ├──────────────────┤────┐   │  │   │certificate_id    │
        │_id (PK)          │    │   │  │   │position (page)   │
        │document_id (FK)◄─┼────┼───┘  │   │timestamp         │
        │signer_id (FK)◄───┼────┼──────┼───┤is_valid          │
        │signature_image   │    │      │   │verified_at       │
        │signature_hash    │    │      │   │...               │
        │certificate_id◄───┼────┼──────┼───┤(relations)       │
        │position          │    │      │   └──────────────────┘
        │timestamp         │    │      │
        │is_valid          │    │      │
        │...               │    │      │
        └──────────────────┘    │      │
                                │      │
                                │      │
                      ┌─────────┘      │
                      │                │
                      ▼                ▼
                ┌──────────────────┐ ┌──────────────────┐
                │signature_audit_log   (optional)
                ├──────────────────┤
                │_id (PK)          │
                │action            │
                │signer_id (FK)    │
                │document_id (FK)  │
                │details (JSON)    │
                │timestamp         │
                │ip_address        │
                │...               │
                └──────────────────┘

KEY RELATIONSHIPS:
  users (1) ──→ (∞) users_certificates
  users (1) ──→ (∞) user_signatures
  users (1) ──→ (∞) documents (owner)
  users (∞) ──→ (∞) documents (signers) via document_signatures
  documents (1) ──→ (∞) document_signatures
  users_certificates (1) ──→ (∞) document_signatures
```

---

## 8. API REQUEST/RESPONSE EXAMPLES

### Example 1: Generate Certificate

**REQUEST:**
```http
POST /api/certificates/generate
Authorization: Bearer <JWT_TOKEN>
Content-Type: application/json

{
  "user_id": "65a1f2b3c4d5e6f7g8h9i0j1"
}
```

**RESPONSE (201 Created):**
```json
{
  "success": true,
  "message": "Certificate generated successfully",
  "certificate": {
    "certificate_id": "cert_65a1f2b3c4d5e6f7g8h9i0j1",
    "public_key": "-----BEGIN PUBLIC KEY-----\nMIIBIjANBg...\n-----END PUBLIC KEY-----",
    "certificate_pem": "-----BEGIN CERTIFICATE-----\nMIIDXTC...\n-----END CERTIFICATE-----",
    "fingerprint": "SHA256:AB:CD:EF:12:34:56:78:90...",
    "validity": {
      "not_before": "2026-02-23T10:30:00Z",
      "not_after": "2031-02-23T10:30:00Z"
    },
    "status": "active"
  }
}
```

---

### Example 2: Sign Document

**REQUEST:**
```http
POST /api/documents/65a1f2b3c4d5e6f7g8h9i0j1/sign
Authorization: Bearer <JWT_TOKEN>
Content-Type: application/json

{
  "signature_id": "sig_abc123def456",
  "signature_image": "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAA...",
  "position": {
    "page": 1,
    "x": 150,
    "y": 200,
    "width": 200,
    "height": 100
  },
  "certificate_id": "cert_65a1f2b3c4d5e6f7g8h9i0j1"
}
```

**RESPONSE (200 OK):**
```json
{
  "success": true,
  "message": "Document signed successfully",
  "signature": {
    "signature_id": "docsig_xyz789abc123",
    "document_id": "65a1f2b3c4d5e6f7g8h9i0j1",
    "signer_id": "user_12345",
    "signed_at": "2026-02-23T14:45:30.123Z",
    "position": {
      "page": 1,
      "x": 150,
      "y": 200,
      "width": 200,
      "height": 100
    },
    "certificate_fingerprint": "SHA256:AB:CD:EF:12:34:56:78:90...",
    "is_valid": true
  }
}
```

---

### Example 3: Verify Document

**REQUEST:**
```http
POST /api/documents/65a1f2b3c4d5e6f7g8h9i0j1/verify
Authorization: Bearer <JWT_TOKEN>
```

**RESPONSE (200 OK):**
```json
{
  "success": true,
  "document": {
    "document_id": "65a1f2b3c4d5e6f7g8h9i0j1",
    "title": "Contract Agreement",
    "is_valid": true,
    "all_signatures_valid": true
  },
  "signatures": [
    {
      "signature_id": "docsig_xyz789abc123",
      "signer": {
        "signer_id": "user_12345",
        "name": "John Doe"
      },
      "signed_at": "2026-02-23T14:45:30.123Z",
      "is_valid": true,
      "verification": {
        "signature_valid": true,
        "certificate_valid": true,
        "certificate_not_expired": true,
        "certificate_not_revoked": true,
        "hash_match": true
      }
    },
    {
      "signature_id": "docsig_xyz789def456",
      "signer": {
        "signer_id": "user_67890",
        "name": "Jane Smith"
      },
      "signed_at": "2026-02-23T15:22:10.456Z",
      "is_valid": true,
      "verification": {
        "signature_valid": true,
        "certificate_valid": true,
        "certificate_not_expired": true,
        "certificate_not_revoked": true,
        "hash_match": true
      }
    }
  ]
}
```

---

## 9. STATE DIAGRAM: DOCUMENT LIFECYCLE

```
                    DOCUMENT STATUS FLOW

┌──────────────┐
│   DRAFT      │  (Just uploaded, not sent for signing yet)
└──────┬───────┘
       │
       │ (Send for signature)
       ▼
┌──────────────────────┐
│ PENDING_SIGNATURE    │  (Waiting for someone to sign)
└──────┬───────────────┘
       │
       ├─────────────────────────────────────────┐
       │                                         │
       │ (Single signer)    (Multi-signer)       │
       ▼                    ▼                    │
┌──────────────┐    ┌────────────────┐          │
│  SIGNED      │    │ PARTIALLY_     │          │
│  (Complete)  │    │ SIGNED         │          │
└──────────────┘    └────────┬───────┘          │
                             │                  │
                             │ (Next signer)    │
                             ▼                  │
                      ┌──────────────┐          │
                      │ PENDING_     │          │
                      │ NEXT_SIGNER  │◄─────────┤
                      └──────┬───────┘          │
                             │                  │
                             │ (Last signer)    │
                             ▼                  │
                      ┌──────────────┐          │
                      │  SIGNED      │          │
                      │  (Complete)  │          │
                      └──────┬───────┘          │
                             │                  
       ┌─────────────────────┴─────────────────┐
       │                                       │
       │ (Archive/Download)                    │ (Reject)
       ▼                                       ▼
┌──────────────┐                       ┌──────────────────┐
│  ARCHIVED    │                       │ REJECTED         │
│              │                       │                  │
│ Read-only    │                       │ Can restart      │
│ downloadable │                       │ signing          │
└──────────────┘                       └──────┬───────────┘
                                               │
                                               │
                                               ▼
                                        ┌──────────────┐
                                        │ DRAFT        │
                                        │              │
                                        │ Back to start │
                                        └──────────────┘
```

---

Created: February 23, 2026
Status: Complete - Ready for Implementation
